# Inputs

Place large Chronos inputs here. These files are intentionally not copied
into Git:

- `members-2.csv`
- `mean_and_std_healpix.fits`
- optional MIST CMD files under `mist_isochrones/`

On your laptop, the current source files are likely:

```bash
~/Downloads/members-2.csv
~/Downloads/mean_and_std_healpix.fits
```
